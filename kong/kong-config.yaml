apiVersion: v1
kind: ConfigMap
metadata:
  name: kong-config
data:
  kong.yaml: |
    _format_version: "1.1"
    services:
      - name: example-service
        # URL del servicio backend de ejemplo (reemplázala según tus necesidades)
        url: http://example.com
        routes:
          - name: example-route
            paths:
              - /
            plugins:
              - name: oidc
                config:
                  client_id: kong-client
                  client_secret: secret
                  # La URL de descubrimiento usa "keycloak" como hostname; asegúrate que el Service Keycloak esté correctamente configurado.
                  discovery: http://keycloak:8080/auth/realms/master/.well-known/openid-configuration
                  scopes:
                    - openid
                    - profile
                    - email
